# Runtime dependencies for the deployed function, installed by Vercel.
#
# The catalog half of the API still reads from a JSON file and touches no
# database -- app/seed/reader.py is kept free of SQLAlchemy so an anonymous
# request never loads the ORM. Accounts changed the rest: saved orders, watch
# progress and sessions all live in Postgres, so the driver and the auth
# libraries ship too.
#
# Still deliberately absent: alembic (migrations are run from a dev machine
# against the direct, non-pooled host, never from the function) and the
# developer tooling in backend/pyproject.toml's dev extra.
fastapi>=0.141
pydantic>=2.10
pydantic-settings>=2.7

# Per-user data.
sqlalchemy>=2.0.36
psycopg[binary]>=3.2

# Sessions. Plain pyjwt rather than pyjwt[crypto]: HS256 signing needs no
# `cryptography`, and leaving it out keeps the bundle well clear of the size
# limit.
pyjwt>=2.10
pwdlib[argon2]>=0.2.1
email-validator>=2.2
